apiVersion: apps/v1
kind: Deployment
metadata:
  name: appointment-deployment
  namespace: squad-9
  labels:
    app: appointment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: appointment
  template:
    metadata:
      labels:
        app: appointment
    spec:
      containers:
        - name: appointment
          image: IMAGE_URI=539935451710.dkr.ecr.us-east-1.amazonaws.com/appointment:latest
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  namespace: squad-9
spec:
  selector:
    app: appointment
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer  # This will create an external load balancer for the service
