apiVersion: apps/v1
kind: Deployment
metadata:
  name: patient-deployment
  namespace: squad-9
  labels:
    app: patient
spec:
  replicas: 2
  selector:
    matchLabels:
      app: patient
  template:
    metadata:
      labels:
        app: patient
    spec:
      containers:
        - name: patient
          image: IMAGE_URI=539935451710.dkr.ecr.us-east-1.amazonaws.com/patient:latest
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: patient-service
  namespace: squad-9
spec:
  selector:
    app: patient
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer  # This will create an external load balancer for the service
